class LevelConfig {
  final int level;
  final int neutralStarCount;
  final String text;

  const LevelConfig({
    required this.level,
    required this.neutralStarCount,
    required this.text,
  });
}
